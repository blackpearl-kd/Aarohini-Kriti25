<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Consumer Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f3f4f6;
            padding: 20px;
        }
        .tab-content {
            background-color: #ffffff;
            border-radius: 5px;
            padding: 20px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        .item-card {
            background-color: #f9fafb;
            border: 1px solid #e5e7eb;
            border-radius: 10px;
            padding: 15px;
            margin-bottom: 15px;
        }
        .item-card h5 {
            color: #16a34a;
        }
        .item-card p {
            margin: 5px 0;
        }
        .item-card button {
            background-color: #16a34a;
            color: white;
            border: none;
            padding: 8px 12px;
            border-radius: 5px;
            cursor: pointer;
        }
        .item-card button:hover {
            background-color: #15803d;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center mb-4">Welcome, Consumer</h1>
        
        <!-- Tab Navigation -->
        <ul class="nav nav-tabs" id="consumerTabs" role="tablist">
            <li class="nav-item" role="presentation">
                <button class="nav-link active" id="buy-tab" data-bs-toggle="tab" data-bs-target="#buy" type="button" role="tab" aria-controls="buy" aria-selected="true">Buy</button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="orders-tab" data-bs-toggle="tab" data-bs-target="#orders" type="button" role="tab" aria-controls="orders" aria-selected="false">Orders</button>
            </li>
            <!-- Add more tabs as needed -->
        </ul>

        <!-- Tab Content -->
        <div class="tab-content mt-3" id="consumerTabContent">
            <!-- Buy Tab -->
            <div class="tab-pane fade show active" id="buy" role="tabpanel" aria-labelledby="buy-tab">
                <h2 class="mb-4">Available Items</h2>
                {{#if items.length}}
                    {{#each items}}
                        <div class="item-card">
                            <h5>{{this.name}}</h5>
                            <p><strong>Price:</strong> ₹{{this.price}}</p>
                            <p><strong>Quantity:</strong> {{this.quantity}}</p>
                            <p><strong>Description:</strong> {{this.description}}</p>
                            <form action="/buyItem/{{this._id}}" method="post">
                                <button type="submit">Buy Now</button>
                            </form>
                        </div>
                    {{/each}}
                {{else}}
                    <p>No items available at the moment.</p>
                {{/if}}
            </div>

            <!-- Orders Tab -->
            <div class="tab-pane fade" id="orders" role="tabpanel" aria-labelledby="orders-tab">
                <h2 class="mb-4">Your Orders</h2>
                <p>No orders to show yet.</p>
                <!-- Dynamically populate orders here in the future -->
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
